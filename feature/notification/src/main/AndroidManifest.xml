<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <receiver android:name=".MarkHabitCompleteReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="mark-habit-complete"/>
            </intent-filter>
        </receiver>
    </application>
</manifest>